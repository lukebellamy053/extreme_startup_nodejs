/**
 * @param question
 * @returns {string}
 */
const getAnswer = (question) => {
    console.log(`Receieved question ${question}`)
    const mathRegex = /.*(\d{2}) \+ (\d{2}).*/

    return 'Luke';
}

module.exports = {
    getAnswer
};
